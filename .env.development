# Test appKey for globalState persistence (doc §4.8: extensionConfig.appKey).
# think_studio is the test value (same as EpicWeb's APP_KEY). Prod value is
# assigned by Epic at onboarding — set it here or via CI env before build.
VITE_EPIC_APP_KEY=think_studio
